# Ansible Dev Laptop configuration

## Installation

the file `./hosts` can be used to provision `localhost`.

## Testing

`./Vagrantfile` may be used to test the config management scripts on Ubuntu 16.04.

